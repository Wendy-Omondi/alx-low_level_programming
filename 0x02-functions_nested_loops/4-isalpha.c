#include "holberton.h"
/**
* main - Checks for alphabetic character
*
* Return: Always 0 (Success)
*/
int main(void)
{
int _isalpha(int c);

int c = 0;
int w = '1';
int y = '0';
if (_isalpha(c))
_putchar(w);
else
_putchar(y);
_putchar('\n');
return (0);
}
