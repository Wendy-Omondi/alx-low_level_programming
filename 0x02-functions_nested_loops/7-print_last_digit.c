#include "holberton.h"

/**
* print_last_digit - Entry point
*@n: tested integer
* Return: Always 0 (Success)
*/

int print_last_digit(int n);
{
int w;
if (n < 0)
{
a = -1 * (n % 10);
_putchar(a + '0');
return (w);
}
Else
{
mv = n % 10;
return (w);
}
}
