#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
int w;
for (w = 0; w <= 9; w++)
{
putchar (w);
}
int x;
for (x = 'a'; x <= 'f'; x++)
{
putchar(x);
}
return (0);
}
